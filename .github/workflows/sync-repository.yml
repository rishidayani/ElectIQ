name: Synchronize repository

on:
  workflow_dispatch:

  # August 12 at 4:00 PM IST (10:30 AM UTC)
  schedule:
    - cron: "35 10 12 8 *"

permissions:
  contents: read

concurrency:
  group: full-repository-sync
  cancel-in-progress: false

jobs:
  synchronize:
    if: github.repository == 'rishidayani/ElectIQ'

    runs-on: ubuntu-latest

    steps:
      - name: Clone source repository
        env:
          SOURCE_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail

          git clone --bare \
            "https://x-access-token:${SOURCE_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" \
            source.git

      - name: Synchronize target repository
        working-directory: source.git
        env:
          TARGET_TOKEN: ${{ secrets.TARGET_TOKEN }}
        run: |
          set -euo pipefail

          git push --prune \
            "https://x-access-token:${TARGET_TOKEN}@github.com/rishi-dayani/ElectreIQ.git" \
            "+refs/heads/*:refs/heads/*" \
            "+refs/tags/*:refs/tags/*"
